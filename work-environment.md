# 작업 환경 안내 (Cursor Claude Code용)

> 이 문서는 사용자가 작업 환경을 변경했음을 Cursor 안의 Claude Code에 알리고,
> 두 환경에서 작업할 때 따라야 할 협업 규칙을 정리한 문서입니다.

## 배경

지금까지는 Cursor 안의 Claude Code 한 곳에서 모든 작업을 진행해왔습니다.
하지만 다음과 같은 한계가 있었습니다:

- 학교 121개 측정 같은 **오래 걸리는 작업**이 돌아가는 동안 다른 작업을 병행하기 어려움
- 노트북 전원을 끄거나 Cursor를 닫으면 진행 중인 작업이 중단됨
- 자리를 비우는 동안에도 작업이 계속 돌아야 하는 케이스가 있음

이를 해결하기 위해 **WSL2 + tmux** 백그라운드 실행 환경을 별도로 구축했습니다.

## 오늘 완료한 환경 구축

1. **WSL2 (Ubuntu) 설치** — Windows 안에 Linux 환경 구축
2. **DNS 이슈 해결** — WSL2가 외부 통신 못 하던 문제를
   `/etc/wsl.conf` + `/etc/resolv.conf` 수동 설정으로 해결
3. **tmux 설치** — 터미널을 닫아도 세션이 백그라운드에 살아있게 해주는 도구
4. **WSL2 안에 Node.js 20 + Claude Code 별도 설치**
   — Windows 쪽 Claude Code와는 독립된 환경
5. **tmux + Claude Code 실전 워크플로우 검증 완료**
   — 세션 생성, Claude Code 실행, detach (`Ctrl+B, D`),
     재접속 (`tmux attach`), 세션 정리까지 직접 테스트 완료

## 환경 공유 구조

두 환경은 **같은 프로젝트 폴더와 git 상태를 공유**합니다.

- Windows 경로: `C:\Users\sooyo\workspace\school-meal`
- WSL2 경로: `/mnt/c/Users/sooyo/workspace/school-meal`

따라서 tmux에서 한 작업의 결과물(파일 생성/수정, commit)이
Cursor 좌측 파일 탐색기와 Source Control 탭에 자동으로 반영됩니다.
별도 동기화 작업은 불필요합니다.

## 협업 시 지켜야 할 규칙

### 1. 같은 파일 동시 수정 금지

두 환경의 Claude Code는 서로의 작업을 모릅니다.
파일 충돌을 방지하기 위해:

- 작업 영역을 분리할 것 (다른 디렉토리/파일)
- 가능하면 다른 브랜치에서 진행
- tmux 작업 시작 전에 어떤 파일/디렉토리를 다룰지 사용자에게 명시할 것

### 2. dev 서버는 한 곳에서만

`npm run dev`는 포트가 겹치므로 한쪽에서만 실행합니다.
보통 Cursor 쪽에서 띄우고, tmux 쪽 작업은 그 dev 서버를 호출하는 방식.

### 3. git 동기화 명시

tmux에서 commit한 변경사항은 Cursor에서 자동 인식되지 않을 수 있습니다.
작업 분기점에서 사용자가 "여기서 git pull 한 번 해줘" 같은 지시를 줄 수 있으니
참고할 것.

## 작업 지시 컨벤션

사용자가 다음과 같이 지시할 예정입니다:

| 사용자의 표현 | 의미 |
|---|---|
| 일반 작업 지시 | Cursor에서 진행 |
| "이건 tmux에서 돌릴 작업이야" | 사용자가 별도로 WSL2 세션에서 실행할 예정. 스크립트나 명령어를 잘 정리해서 전달할 것 |
| "백그라운드에 던지고 싶은데" | 긴 작업이면 tmux용으로 정리해줄 것 |

판단이 애매한 경우 어느 환경에서 실행하는 게 적합한지 의견을 주는 것도 환영합니다.

## 결과 확인 방식

tmux 세션의 작업 결과는 결국 파일/git에 반영되므로 Cursor에서:

- 좌측 파일 탐색기로 새 파일/변경된 파일 확인
- Source Control 탭(`Ctrl+Shift+G`)으로 diff 확인
- 통합 터미널에서 `git log`, `git status`, `git diff`로 확인
- 필요하면 통합 터미널에서 `wsl` → `tmux attach -t <세션명>` 으로
  tmux 세션 화면을 직접 들여다보기도 가능

## 정리

앞으로 작업이 들어오면 다음 기준으로 적절히 분배해주세요:

- 짧고 인터랙티브한 작업 → Cursor에서 직접 처리
- 길고 무인 실행 가능한 작업 → tmux용 스크립트/명령으로 정리해서 사용자에게 전달
- 사용자가 명시적으로 환경을 지정하면 그 지시를 따를 것
